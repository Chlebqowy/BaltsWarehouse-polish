[manifest]
version = "1.0.0"
priority = -2147483648

[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "local game_over = true"
position = "before"
payload = '''
local return_to_shop = false
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = "if G.GAME.round_resets.ante == G.GAME.win_ante and G.GAME.blind:get_type() == 'Boss' then"
position = "before"
payload = '''

for i = 1, #G.vouchers.cards do
    local eval = nil
    eval = G.vouchers.cards[i]:calculate_joker({end_of_round = true, game_over = game_over})
    if eval then
        return_to_shop = return_to_shop or eval.return_to_shop
        card_eval_status_text(G.vouchers.cards[i], 'vouchers', nil, nil, nil, eval)
    end
end
if return_to_shop then
    print("Returning to shop...")
    WAREHOUSE.return_to_shop(return_to_shop)
    return true
end
'''
match_indent = false
times = 1
